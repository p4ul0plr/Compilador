program teste ;

!    var cache : array [1 .. 100] of integer;
    var i : integer;
    var a : integer;
    var b : integer;
    var c : integer;
    var d : integer;

begin

!   i := 1;

!   while(i <= 100) do
!    begin
!      cache[i] := 1;
!      i := i+1;
!   end;

!   cache[1] := 1;
!   cache[2] := 1;

!    if(cache[x] <> 1) then
!        begin
!            fib := cache[x];
!        end
!   else
!        cache[x] := p+pp;

end.